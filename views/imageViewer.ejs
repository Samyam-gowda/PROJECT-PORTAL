<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= folderName %> - Image Viewer</title>
    <style>
        /* Basic styles for the image viewer */
        body {
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            align-items: center;
            background-color: #f0f0f0;
        }
        img {
            max-width: 90%;
            max-height: 90vh;
            margin: 10px;
            border: 2px solid #ccc;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <!-- <p><h1>Images in <%= folderName %></h1></p><br><br> -->
    <% images.forEach(image => { %>
        <img src="<%= image %>" alt="Image">
    <% }) %>
    <a href="/gallery">Back to Gallery</a>
</body>
</html>