<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Image Gallery</title>
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* Body styling */
        body {
            font-family: Arial, sans-serif;
            background-color: #f8f9fa;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            min-height: 100vh;
            color: #333;
        }

        /* Navigation Bar */
        .navbar {
            width: 100%;
            background-color: #007BFF;
            display: flex;
            align-items: center;
            padding: 15px 20px;
            justify-content: space-between;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .navbar img {
            height: 50px;
        }

        .navbar h1 {
            color: white;
            font-size: 20px;
            margin-left: 10px;
        }

        /* Main container */
        .container {
            margin-top: 40px;
            width: 90%;
            max-width: 900px;
            text-align: center;
        }

        h2 {
            color: #e06f13;
            margin-top: 20px;
            font-size: 22px;
            text-transform: uppercase;
            border-bottom: 2px solid #0f1f2f;
            display: inline-block;
            padding-bottom: 5px;
        }

        /* List container */
        ul {
            list-style: none;
            padding: 0;
            margin: 20px 0;
            display: flex;
            flex-wrap: wrap;
            justify-content: center;
            gap: 15px;
        }

        /* List items */
        li {
            background-color: #ffffff;
            border-radius: 8px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            padding: 15px;
            width: 220px;
            text-align: center; 
            transition: transform 0.2s ease-in-out;
        }

        li:hover {
            transform: scale(1.05);
        }

        /* Links in the list */
        a {
            text-decoration: none;
            font-size: 16px;
            color: #d81b60;
            font-weight: bold;
            display: block;
            padding: 10px;
        }

        a:hover {
            color: #0056b3;
            text-decoration: underline;
        }

        /* Back to Upload Page */
        .back-link {
            display: inline-block;
            margin-top: 30px;
            text-decoration: none;
            font-size: 16px;
            color: #007BFF;
            font-weight: bold;
        }

        .back-link:hover {
            color: #0056b3;
            text-decoration: underline;
        }

        /* Responsive Design */
        @media (max-width: 600px) {
            li {
                width: 100%;
            }
        }
    </style>
</head>
<body>

    <!-- Navbar -->
    <div class="navbar">
        <img src="/images/logo.webp" alt="College Logo">
        <h1>College Project Repository</h1>
    </div>

    <!-- Main Content -->
    <div class="container">

        <% for (const faculty in galleryData) { %>
            <h2><%= faculty %></h2>
            <ul>
                <% galleryData[faculty].forEach(folder => { %>
                    <li>
                        <a href="/gallery/<%= faculty %>/<%= folder.folderName %>"><%= folder.folderName %></a>
                    </li>
                <% }) %>
            </ul>
        <% } %>

        <a href="/" class="back-link">← Back to Upload Page</a>
    </div>

</body>
</html>
